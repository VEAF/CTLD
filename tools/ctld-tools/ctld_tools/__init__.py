"""ctld-tools — CTLD configuration authoring & generation.

Treats the CTLD engine configuration as data (YAML) and generates the Lua
consumed by the build. Isolated poetry sub-project under tools/; follows the
VMCT (VEAF-Mission-Creation-Tools) Python conventions.
"""

__version__ = "0.1.0"

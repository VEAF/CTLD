# 04c — troop-groups editor (loadableGroups)

Status: 📋 todo
Type: tool (frontend) + test

Edit `loadableGroups` (list of troop-group entries) in the Data screen.

- Per-entry fields: `name`, and the unit counts `inf` / `mg` / `at` / `aa` / `mortar` / `jtac`.
  Add / remove / reorder groups.
- Field tooltips from the schema `tableFields` descriptions (EN/FR).

Files: `tools/ctld-tools/web/**`. Depends on: 04a.

# Backlog — CTLD

Local markdown backlog. One lot = one directory `<LOT-ID>/` (`PRD.md` + `tickets/`). This file is
the hand-maintained index. See `dev/agents/issue-tracker.md` for conventions and
`dev/agents/triage-labels.md` for the `Status:` vocabulary.

## Program — re-tooling CTLD on the VMCT model

The codebase is mature (v2.0.0). This program installs a professional process/tooling layer around
it and migrates DCS integration testing from Witchcraft to VEAF-dcs-bridge. PRDs and tickets are
authored **per lot, when the lot is started** (not in batch).

### Active lots

| Lot | Status | Description | Branch |
|-----|--------|-------------|--------|
| `FIX-INSTALL-SOUND-ORPHANS` | merged (PR #96) | Two defects reported by **Zip** minutes apart, using the `2.0.0-rc4` exe on a real mission — both capabilities that shipped and that the mission never saw. **The installed beacon sounds vanish**: `install()` wrote both `.ogg` into `l10n/DEFAULT/` and declared neither, on the correct-but-incomplete ground that a sound needs no resource key to be *played*. It needs one to *survive* — the Mission Editor rebuilds the archive from its own model on save and drops orphan files, so the beacons fall silent with nothing to explain it, the exact failure `FEAT-ONE-CLICK-INSTALL` existed to end. Fixed with the preload idiom the VEAF mission set already uses and this repo's own test mission carries (`ResKey_Action_10/11`): a key per sound plus a MISSION START `a_out_sound` reference, **646 occurrences across 491 real missions**, copied rather than invented — and *not* the per-coalition variant first suggested, since `coalitionlist` only ever takes `blue` or `red` (no neutral value in those 491 missions), so "a coalition with no unit" cannot be chosen generically. **And no way to open a `.miz`**: `load_path` has read a mission since rc4, but the dialog filtered on `*.yaml *.yml` and the button said "config **file**", making it reachable only via "All files" — a gap CI could not catch, the native dialog being interactive. | `fix/tool-open-miz` |
| `FIX-TOOL-I18N-LANG` | merged (PR #92) | Reported by **FullGas**: CTLD's interface language could not be set from the tool. Neither the engine nor the schema was at fault — `CTLD_i18n` resolves it through `ctld.gs("i18n_lang")` and the schema declares it with `default: en` and `choices: [en, fr, es, ko]`. The app never showed it because it lists **catalogue** keys, and this is the only setting of ~150 deliberately absent from the catalogue: a scalar there would make it a *parameter* under ADR 0011 Addendum 1, so completeness would demand it and every rc1–rc3 config would report a missing setting at startup. A regression from the TUI → web app move, the TUI having listed schema keys. Fixed by offering schema-declared settings the catalogue does not carry, rather than by cataloguing this one. | — |
| `FEAT-ONE-CLICK-INSTALL` | merged (PR #92) | **The Mission Maker's entry point becomes the tool**: download `ctld-tools.exe`, run it, and the engine, the beacon sounds, the configuration and the triggers all go into the `.miz` — the VMCT model. Today that is five manual steps, and step 3 bites hardest: `beacon.ogg` / `beaconsilent.ogg` are **not release assets** (repo-only, in `assets/`), so a mission ships with silent beacons and nothing says why. Decided: the exe **carries** CTLD + the sounds rather than downloading them (+7% on a 21 MB exe, works offline, an exe can only install its own release's engine — and the `--add-data` mechanism already bundles the YAML and the schema); everything lands as **files** with `DO SCRIPT FILE` triggers, config before engine; and the tool can **read a configuration back out of a `.miz`**, which is trivial once the config travels as a file instead of an escaped Lua string inside `mission`. Read VMCT's mission builder for the resource-key plumbing rather than reinventing it. | — |
| `FIX-SPEC-ISOLATION` | merged (PR #94) | `aircraft_capabilities_spec` passed locally and failed in CI: `troop_manager_spec` empties `loadableGroups` and nulls `CTLDTroopManager._instance` in a `before_each` and **never restores them**, so every spec running after it sees an emptied catalogue. FullGas fixed the consumer side in PR #91; the producer is still there — **9 mutations with zero `after_each` in `troop_manager_spec`, 8 in `type_collector_spec`**, `menu_gating_spec` being the only one of the three that restores. Worth fixing at the producer because "which spec runs first" is unknowable: busted takes the order the filesystem gives (a directory hash on Linux), while the local runner sorts by name and is therefore **blind to this class of bug** — which is why it only failed in CI. Cheap proof: a reversed-order run must agree with a forward one. | — |
| `FEAT-TOOL-VERSION-AND-DOCS` | merged (PR #93) | A help button worth pressing, linking to the documentation **of the tool's own version** — which needs two missing bricks. The tool does not know its version (`pyproject.toml` still says `0.1.0`, the API declares `version="2.0"` as a literal, no `--version`), and the published docs have exactly one version (`versions.json` = `dev` only; `docs.yml` deploys on push to `develop`, nothing on a tag). Decided: one source of truth — the tool reports `ctld.VERSION` — plus `mike deploy` on `published-v*` tags, where a **stable** moves the `latest` alias and a **pre-release does not**, same discipline as `published-latest` for downloads. Until stable ships, the link rule is `dev` for any `-rc` version and the exact `x.y.z` otherwise: honest today, correct by itself the day a stable is tagged. | — |
| `CHORE-RELEASE-INSTALL-NOTES` | merged (PR #92) | Every release page lists four assets and says nothing about what to do with them. The `release` skill gains a required **installation** section, opening the notes: download the exe (the only file needed), run it, install into your `.miz` — with one line for the manual path, linking the docs rather than duplicating them. **Deliberately last** of the three install lots: it documents the journey `FEAT-ONE-CLICK-INSTALL` builds, and a notice written first is a notice that is wrong on publication day. | — |
| `FIX-SHIP-ZONE-ANCHOR-PARITY` | merged (PR #79) | A `troopZones` entry backed by a **ship** is snapshotted at init, so a carrier steams away and leaves its pickup point in the water. v1 re-resolved the position on **every** check (`ctld.inPickupZone`, 200 m radius) — this is an undeclared parity deviation, not a design choice, and it contradicts the lazy-resolution principle `FEAT-MOVING-ZONE` established. The anchor is even already known: `CTLDTroopZone` carries `_anchorUnitName` and `isAnchorAlive()`, but `getCenter()` never consults them, unlike `CTLDLogisticZone:getCenter()`. Fix is the missing `linkedUnit` branch. Blocks `FEAT-VMCT-INTEGRATION` ticket 02. | — |
| `FEAT-VMCT-INTEGRATION` | merged (PR #80) | What CTLD 2 must expose so the VEAF Mission Creation Tools can drop their v1 engine. VMCT switches to CTLD 2 in one move, moves its configuration out to a dedicated `ctld-config.yaml` injected by its build, and keeps only the bridge code by which its runtime modules talk to CTLD. Auditing those bridges found three gaps, all the same shape — **a caller that is not a pilot in a cockpit**: logistic points cannot be declared **by unit type** (01) nor ship pickup points (02), so 50 lines of mission-scanning live in VMCT for want of a hook, hand-written ten unit names at a time by everyone else; and **no beacon can be created without a `transport` and a `player`** (03), although `dropBeacon` already takes an `overridePosition` and an `isFOB` — a scripted FARP simply cannot carry a beacon. Ticket 02 also fixes the existing ship-backed troop zone, snapshotted at init and therefore left behind by a carrier under way. | — |
| `FEAT-AIRCRAFT-CAPABILITIES` | merged (PR #86) | Five **stock** DCS aircraft have no `capabilitiesByType` entry — `SA342L`, `SA342M`, `SA342Mistral`, `SA342Minigun`, `Yak-52` — so their pilots lose crates, troops, beacons and smoke. The fourth and last gap from the VMCT integration audit (the others shipped in #79 and #80), but not a VMCT problem: VMCT has carried these five in production for years, and any mission maker flying a Gazelle hits the same wall. Two corrections came out of the reading: an aircraft without an entry does **not** lose its CTLD menu (the root, Check Cargo, RECON and JTAC status all survive — only the transport half is gated), which the published documentation gets wrong; and **`Ka-50` / `Ka-50_3` deliberately get no entry** — v1 gave them the global troop limit and both actions through two *missing* table entries, and the only thing an all-false entry would add is the beacon action. | — |
| `FIX-AIZONE-NAME-COLLISION` | merged (PR #88) | An `aiZones` entry whose `dcsZoneName` is already a registered troop zone is **skipped in silence** — no WARN, no NOTICE, no log line — leaving the mission maker an AI zone that does nothing. Reachable by accident: a TRZ registers under its **parsed** name, so `TRZ_dropzone1_B_0_nil_0` occupies the key `dropzone1` and an `aiZones` entry on a genuinely different ME zone called `dropzone1` collides with it. The obvious way in is the workaround the migration guide documents — superimposing an inert `TRZ_` marker on an AI drop-off zone, which is never smoked — where naming the marker after the AI zone disables it. `_validateZoneNames` already reports four other `entry ignored` cases as ERROR; this is the only one that says nothing. Found while reading for `FIX-DROPOFFZONES-PARITY` ticket 01. | — |
| `FIX-VALIDATE-MODTYPES` | merged (PR #87) | `modTypes` has exactly one job — the schema says listing a type there "is what stops validation rejecting it as unknown to DCS" — and `ctld-tools validate` does not honour it: `_validate_crates` compares every crate `unit` to the datamine set alone, so a **modded crate is an ERROR that blocks export**, which is precisely what declaring it was supposed to prevent (reproduced). The Lua side has it right, excusing `CTLDTypeCollector`'s `extras`; the two layers disagree and the blocking one is wrong. `FEAT-VMCT-INTEGRATION` added a second, correct behaviour for the type lists rather than widen the bug, so one file now holds both — flagged in PR #80 and left for this lot. | — |
| `FIX-DROPOFFZONES-PARITY` | merged (PR #83) | A v1 config's `dropOffZones` is **read by the legacy monolith and by nothing in `src/`** — an AI transport that used to auto-unload there simply stops, with no error, no NOTICE, no log line. Found by the doc audit for `2.0.0-rc2` (PR #75), which only removed the false claim from `zones.{md,fr.md}`. The setting carries **two** features: AI auto-unload of troops *and* vehicles (`ctld.checkAIStatus` via `ctld.inDropoffZone`), replaced — richer — by an `aiZones` entry with `isDropoff` + `aiDropMode`; and **smoke marking** on the periodic refresh, which `aiZones` appears not to support at all (to confirm — ticket 01). Not a reimplementation: a second spelling for one concept is what the last four lots removed. Delivers a startup NOTICE, a migration-guide entry (the guide has **zero** occurrences of `dropOffZones` today), and a written decision on whether an unmarkable AI zone is a deliberate choice or a gap worth its own lot. | — |
| `FEAT-CONFIG-PARAM-SEMANTICS` | merged (PR #71) | **Lot A** — parameters are always complete, lists are removable. ADR 0011 said "a missing element means intentional removal" without distinguishing the two, and three settings crashed to prove it (`JTAC_*IntervalSeconds` → `t + nil`, `slingCutDestroyHeight` → `agl > nil`). Delivered: **ADR 0011 Addendum 1** (plus the stale "Accepted" status on ADRs 0008/0009, caught by the ticket's own acceptance check); engine resolution of a missing **parameter** from the default, with one on-screen startup NOTICE — **eager, not lazy**, since knowing a snapshot is incomplete requires parsing the default and a miss surfaces long after the report is flushed; **114 duplicate fallbacks deleted** (103 literals + 11 code constants; the 46 `or {…}` list guards stay), of which **five had drifted** — roadmap 2's two plus three parachute-altitude divergences the roadmap never listed (`30`/`50`/`30` vs `152`); and a `validate` completeness rule at `ERROR`, keyed off the default catalogue so schema-only `i18n_lang` is not demanded. Closes roadmap 2 + 3. | `feature/config-param-semantics` |
| `FIX-CATALOGUE-TRUTH` | merged (PR #72) | **Lot B** — the schema and catalogue describe what the engine actually reads. **Must precede lot D.** `tableFields.AIZones` → `aiZones` rewritten from `_loadAIZonesFromConfig` (the dead positional format PR #69 left behind); `GROUND_UNIT` purged (in no map — falls back to GROUND through the error path); `modTypes` `{}`→`[]` (the engine walks it with `ipairs`; the map shape is why the app fell through to `JsonEditor`) + a real description; dead `spawnableCratesModels[*].category` removed (never reaches DCS, and `dynAddStatic` forces `'Cargos'` anyway); hover-height reference frames stated per use — closes roadmap 5 as **no-bug**, wording only. Every closed value set it touches is declared as a `choices` list so a **hand-editing MM** reads the vocabulary in the schema, not in a Svelte component (backward-compatible: `/api/schema` drops unknown keys today, so no flag day with D). Plus `dropCrate` + `maxDropHeight` deleted (closes roadmap 4): no caller, absent from the legacy monolith, and superseded by the fully-wired `parachuteCrates` — the one ticket here that touches `src/`. | — |
| `FEAT-JTAC-DRONE-GLOBALS` | merged (PR #73) | **Lot C** — drone orbit parameters become global settings. FullGas proposed a `specificParams` sub-panel; decided to remove the concept instead. Four globals **rebased on today's per-crate values** so only the chosen change lands: `JTAC_droneAltitude` 4000→**3000**, new `JTAC_droneRadiusNoLase` 2000 / `JTAC_droneRadiusOnLase` 1000 / `JTAC_droneSpeed` 150; `JTAC_droneRadius` retired (one key cannot express both radii). `specificParams` leaves the schema, the catalogue **and** the engine, with a startup NOTICE for stale configs — `validate` and `version-gap` are both blind to a field nested inside a crate entry. Also aligns spawn and orbit altitude, which diverge today. The troop-path `specificParams.task` (Feature I) is untouched. CHANGELOG: in-flight behaviour change. Parallel with A/B/D. | — |
| `FEAT-EDITOR-COVERAGE` | merged (PR #74) | **Lot D** — every complex config key gets a real editor. **Depends on lot B.** `isJTAC` checkbox on every crate row + a **strict** `spawnAs` dropdown offering only `GROUND`/`AIR` (`AIR` is a UI convenience the tool resolves to AIRPLANE/HELICOPTER at save time from a category-enriched `dcs_types.json` — the category is already in the datamine path and currently discarded; `STATIC` not offered, broken by construction through the crate path). Widens the `tableFields` API/frontend contract so **every select reads its options from the schema's `choices`**, never from a literal in a component. New `AiZonesEditor` with the engine's own `VALID_*` vocabularies and the two traps respected (`coalition` is a **string** here, not the numeric `side`; `All` / `-1` made discoverable in the stock maps). Fixed 3-row editor for `spawnableCratesModels`. `modTypes` → existing `StringListEditor`. Out: `mixedSet`, a speculative manned-aircraft `validate` rule. | — |

### Planned lots

**ctld-tools v2 pivot** ([ADR 0011](../dev/adr/0011-complete-yaml-config-and-webapp-tooling.md),
2026-07-24) — supersedes ADR 0008 and ADR 0009 pts 2 & 3. Drops the ops/diff config model, the
`ctld.userSetup` runtime API, the Textual TUI **and** FullGas's tkinter GUI in favour of a
**complete-YAML config** resolved by a plain `or` and a **local web-app** tool (single console exe,
GUI on double-click). Groundwork now retired: `FEAT-USERCONFIG-API`, `CTLD-TOOLS-CONFIG`,
`CTLD-TOOLS-USERCONFIG`, `CTLD-TOOLS-TUI`, `CTLD-TOOLS-TUI-POLISH`, `TUI-EDIT-MODE-UX`, and the
`UX-CTLD-TOOLS-V2` branch. Delivered as three sequenced lots (logic-vs-interface boundary between 2 & 3):

| Lot | Status | Description | Branch |
|-----|--------|-------------|--------|

Future candidates → [`dev/roadmap.md`](../dev/roadmap.md).

### Delivered (socle, this program)

| Lot | Description |
|-----|-------------|
| `REPO-BOOTSTRAP` | New VEAF repo, clean history, Git Flow `develop`/`master` |
| `PROCESS-SCAFFOLD` | Lean `CLAUDE.md`, `.backlog/`, `CONTEXT.md`, `dev/agents/`, `dcs-runtime-debug` skill, history cleanup |
| [`CI-REVAMP`](archive/CI-REVAMP.md) ✅ | CI on `develop`, single build source, coverage ratchet (59%/61.56%), gitleaks, hygiene (PR #1). |
| [`CONTEXT-ADR`](archive/CONTEXT-ADR.md) ✅ | Retroactive ADRs 0001–0005 in `dev/adr/` (PR #4). |
| [`CLAUDE-AUTOMATIONS`](archive/CLAUDE-AUTOMATIONS.md) ✅ | Project hooks (block protected paths, luacheck-on-edit) + subagents lua51/parity (PR #5). |
| [`DCS-DATAMINE-VENDOR`](archive/DCS-DATAMINE-VENDOR.md) ✅ | Vendored DCS type set (not shipped) + offline config type linter (PR #6). |
| [`RELEASE`](archive/RELEASE.md) ✅ | `release` skill + `release.yml` (tag `published-v*`); release job moved out of ci.yml (PR #7). |
| [`DOC-MKDOCS`](archive/DOC-MKDOCS.md) ✅ | mkdocs-material infra (i18n EN+FR, mike) + `docs.yml` → gh-pages live at veaf.github.io/CTLD (PR #8). |
| [`DOC-TECH`](archive/DOC-TECH.md) ✅ | Bilingual `docs/developer/` consolidation (20 EN + 20 FR pages) + workflow page; old sources + `migration/specs/` removed (PR #11). |
| [`DOC-USER-ROLES`](archive/DOC-USER-ROLES.md) ✅ | Bilingual role split of the user guide → `docs/pilot/` + `docs/mission-maker/` (19 EN + 19 FR pages); monolith removed (PR #12). |
| [`DCS-BRIDGE-MCP`](archive/DCS-BRIDGE-MCP.md) ✅ | Migrate DCS integration testing from Witchcraft to VEAF-dcs-bridge: `.mcp.json` + project-local venv, scenario return contract, 79 scenarios + 4 templates migrated, `integration-testing` skill, Witchcraft fully retired (PR #14). Uncovered ~194 dead FullGas relics → `CLEANUP-LEGACY-DCS-TESTS`. |
| [`INTEGRATION-TEST-TAGS`](archive/INTEGRATION-TEST-TAGS.md) ✅ | `-- @tier: auto\|auto-check\|ia` header on the 79 scenarios + 4 templates (43/2/34 split); fixed F-122's unresolved verdict found during the classification audit (PR #15). |
| [`INTEGRATION-TEST-RUNNER`](archive/INTEGRATION-TEST-RUNNER.md) ✅ | Dependency-free `tools/integration-runner/run_scenarios.py`: discovers + tier-filters scenarios, drives them over dcs-serve REST, polls async ones, writes a JUnit report; `--no-ai` mode runs every `auto`/`auto-check` scenario headlessly. 31 stdlib unit tests. Closes the DCS-bridge triptych. |
| [`REINTEGRATE-ORPHAN-TESTS`](archive/REINTEGRATE-ORPHAN-TESTS.md) ✅ | Rebuilt coverage for the 46 features tested only by dead FullGas relics — 6 busted tickets (158 tests, green in CI) + 1 live-DCS ticket (3 scenarios: BCN 30/30, RCN 10/10, VEH 22/22). 10 relics were false orphans (already covered); 6 deferred to FullGas (`dev/fullgas-report.md`). PR #20. |
| [`POST-FULLGAS-FIXES`](archive/POST-FULLGAS-FIXES.md) ✅ | Applied FullGas's review answers: Feature Q whole-vehicle spawn regression, AI-zone stock validation rewrite, U-108 heliport probe fixes, F-117/F-118 recon fixes, `coord_farp-1` mission static, README doc touch-ups. First full live run against real DCS (27/45) surfaced 18 failures, 8 fixed here as a side effect — remaining 10 → `FIX-LIVE-DCS-FAILURES`. PR #22. |
| [`FIX-LIVE-DCS-FAILURES`](archive/FIX-LIVE-DCS-FAILURES.md) ✅ | Triaged the 10 (of 18) failures from the first live run (2026-07-10) not already fixed by `POST-FULLGAS-FIXES` — all 10 turned out to be cross-scenario state contamination, cleared by a mission reload (48/48 green, no src/test change). Also closed the L4 gap on Feature Q's whole-vehicle Request Equipment menu. PR #23. |
| [`SCENE-PLUGINS`](archive/SCENE-PLUGINS.md) ✅ | Pluggable scenes + extracted the mod-dependent Metal FARP into the new [`VEAF/CTLD_plugins`](https://github.com/VEAF/CTLD_plugins) repo (killing its every-mission-start WARN). Scenes are load-position-independent; scene asset validation moved to a design-time busted hard-gate (datamine ∪ `modTypes`); runtime scene audit removed; `requiresCtld` version check. ADRs [0006](../dev/adr/0006-pluggable-scenes.md)/[0007](../dev/adr/0007-design-time-asset-validation.md). CTLD PR #26; plugins repo bootstrapped (PR #1/#2). |
| [`ASSET-VALIDATION-REVAMP`](archive/ASSET-VALIDATION-REVAMP.md) ✅ | Removed `CTLD_modValidator`'s runtime probe-spawn (no more spurious `S_EVENT_BIRTH`/destroy at mission start). Shared `CTLDTypeCollector` (fixes the GROUND `unitType` gate gap); `modTypes` config setting; optional dev-time asset-check companion (`dist/CTLD_asset_check.lua`, no-spawn lookup). ADR [0007](../dev/adr/0007-design-time-asset-validation.md). CTLD PR #27; plugins gate fix PR #3. |
| [`CLEANUP-LEGACY-DCS-TESTS`](archive/CLEANUP-LEGACY-DCS-TESTS.md) ✅ | Purged 194 dead FullGas relics from `tests/dcs/noPlayer/` (dangling `dofile`, absent `ctld_test`, hardcoded paths). FullGas confirmed. 45 live dcs-bridge scenarios unaffected. PR #33. |
| [`USERCONFIG-LOADING`](archive/USERCONFIG-LOADING.md) ✅ | `CTLD_userConfig.lua` removed from build merge; new `CTLD_bootstrap.lua` keeps auto-start in deliverable; userConfig delivered in `dist/` as standalone MM template. PR #32. |

## Dropped lots

| Lot | Status | Reason |
|-----|--------|--------|
| `STYLUA-ADOPTION` | 🚫 wontfix | Adoption would reformat ~500 files for marginal benefit (code already consistent + luacheck-clean), and requires first untangling pre-existing CRLF blobs against a global `core.autocrlf=true`. Not worth it; `stylua.toml` removed. luacheck stays the sole Lua gate. |

## Archived lots

Completed lots are compacted under `archive/<LOT-ID>.md`.

| Lot | Status | Description |
|-----|--------|-------------|
| [`ASSET-VALIDATION-REVAMP`](archive/ASSET-VALIDATION-REVAMP.md) | delivered | Removed `CTLD_modValidator`'s runtime probe-spawn (no more spurious `S_EVENT_BIRTH`/destroy at mission start). |
| [`BUILD-DICT-AI-TRANSLATE`](archive/BUILD-DICT-AI-TRANSLATE.md) | ✅ merged (PR #60) | Traduire automatiquement les stubs i18n vides via l'API Claude Haiku (local, si `ANTHROPIC_API_KEY` dispo) + câbler `ctld.i18n_auditAll()` dans `ctld.startupReport` pour signaler les clés vides au run |
| [`BUILD-DICT-AUTOSYNC`](archive/BUILD-DICT-AUTOSYNC.md) | ✅ merged (PR #59) | Intégrer `generate_i18n_dicts.ps1 -Apply` dans `merge_CTLD.ps1` + hook `pre-push` cross-platform (bloquant sur MISSING, warn sur STALE) + doc activation dans `CLAUDE.md`. |
| [`CATCH-UP-PILOT-SCENARIOS`](archive/CATCH-UP-PILOT-SCENARIOS.md) | ✅ merged (PR #24) | Ran the never-executed `pilotPassive`/`pilotActive` scenarios. |
| [`CHORE-DOC-GATES`](archive/CHORE-DOC-GATES.md) | ✅ merged (PR #39) | Enforce CHANGELOG + backlog-index bookkeeping instead of relying on discipline: CI `changelog-guard` job failing a `src/`-touching PR with no `CHANGELOG.md` edit (escape hatch: `skip-changelog` label) |
| [`CI-REVAMP`](archive/CI-REVAMP.md) | delivered | CI on `develop`, single build source, coverage ratchet (59%/61.56%), gitleaks, hygiene (PR #1). |
| [`CLAUDE-AUTOMATIONS`](archive/CLAUDE-AUTOMATIONS.md) | delivered | Project hooks (block protected paths, luacheck-on-edit) + subagents lua51/parity (PR #5). |
| [`CLEANUP-LEGACY-DCS-TESTS`](archive/CLEANUP-LEGACY-DCS-TESTS.md) | delivered | Purged 194 dead FullGas relics from `tests/dcs/noPlayer/` (dangling `dofile`, absent `ctld_test`, hardcoded paths). |
| [`CONTEXT-ADR`](archive/CONTEXT-ADR.md) | delivered | Retroactive ADRs 0001–0005 in `dev/adr/` (PR #4). |
| [`CTLD-TOOLS-CONFIG`](archive/CTLD-TOOLS-CONFIG.md) | ✅ merged (PR #46) | Lot 2 of `ctld-tools` ([ADR 0009](../dev/adr/0009-external-yaml-authoring-ctld-tools.md)): engine defaults moved out of `CTLD_config.lua` into `src/CTLD_config.yaml` (source of truth, sectioned mm-fac |
| [`CTLD-TOOLS-CORE`](archive/CTLD-TOOLS-CORE.md) | ✅ merged (PR #66) | Lot 2/3 — UI-agnostic tool core: demolish ops model/TUI/tkinter/`reference.json`/`gen-config` + drop `lupa`; complete-catalogue load/edit/save + `validate` (schema + datamine + mixedSet) + version-gap |
| [`CTLD-TOOLS-FINALIZE`](archive/CTLD-TOOLS-FINALIZE.md) | ✅ merged (PR #48) | Finalize `ctld-tools`: **gen-au-build** (`merge_CTLD.ps1` regenerates `CTLD_config_defaults.lua` via `ctld-tools`, now a git-ignored artifact; CI/release gain setup-python; drift check dropped) + buil |
| [`CTLD-TOOLS-MIZ-INJECT`](archive/CTLD-TOOLS-MIZ-INJECT.md) | ✅ merged (PR #50) | `ctld-tools inject`: insert the generated `CTLD_userConfig.lua` into a `.miz` as a rank-1 MISSION START trigger (idempotent), patching `trig`/`trigrules` with index-shift + in-code `[idx]` rewrite (VM |
| [`CTLD-TOOLS-MM-UX`](archive/CTLD-TOOLS-MM-UX.md) | merged (PR #68) | UI/UX pass on `ctld-tools.exe` for the non-technical Mission Maker + a visual identity rooted in the subject (DCS rotary-wing logistics). |
| [`CTLD-TOOLS-TUI`](archive/CTLD-TOOLS-TUI.md) | ✅ merged (PR #52) | Interactive **textual** TUI for `ctld-tools` ([ADR 0009](../dev/adr/0009-external-yaml-authoring-ctld-tools.md)): a MM console that structurally edits the `user-config.yaml`, validates live, and gener |
| [`CTLD-TOOLS-TUI-POLISH`](archive/CTLD-TOOLS-TUI-POLISH.md) | ✅ merged (PR #55) | Make the CTLD interface language (`i18n_lang`) a first-class MM setting (defaults + `ctld.tr` via `ctld.gs`, schema `choices`); it was a bare global, unsettable via user-config and absent from the pic |
| [`CTLD-TOOLS-USERCONFIG`](archive/CTLD-TOOLS-USERCONFIG.md) | ✅ merged (PR #47) | Lot 3 of `ctld-tools` ([ADR 0009](../dev/adr/0009-external-yaml-authoring-ctld-tools.md)): MM volet — `validate` (user-config.yaml against the reference + datamine, clear report with suggestions) + `g |
| [`CTLD-TOOLS-WEBAPP`](archive/CTLD-TOOLS-WEBAPP.md) | ✅ merged (PR #67) | Lot 3/3 — local web app over the lot-2 core: schema-driven editors, 13 families (FullGas's 12 + Parachute) + Parameters/Data split fully editable (crates/troops/aircraft+datamine picker/zones/lists/we |
| [`DCS-BRIDGE-MCP`](archive/DCS-BRIDGE-MCP.md) | delivered | Migrate DCS integration testing from Witchcraft to VEAF-dcs-bridge: `.mcp.json` + project-local venv, scenario return contract, 79 scenarios + 4 templates migrated, `integration-testing` skill, Witchc |
| [`DCS-DATAMINE-VENDOR`](archive/DCS-DATAMINE-VENDOR.md) | delivered | Vendored DCS type set (not shipped) + offline config type linter (PR #6). |
| [`DEV-LOCAL-MIZ`](archive/DEV-LOCAL-MIZ.md) | ✅ merged (PR #40) | Kill the hardcoded `CTLD.lua` path in the shared martyr miz: MISSION START trigger loads via `CTLD_DEV_ROOT` env var (de-sanitized DCS), hardened with explicit on-screen failure; delete the dead `ctld |
| [`DOC-MKDOCS`](archive/DOC-MKDOCS.md) | delivered | mkdocs-material infra (i18n EN+FR, mike) + `docs.yml` → gh-pages live at veaf.github.io/CTLD (PR #8). |
| [`DOC-NAV-I18N`](archive/DOC-NAV-I18N.md) | ✅ merged (PR #53, #54) | Translate the mkdocs site navigation to French (`nav_translations` under the FR locale) and harmonise every FR page H1 to the `French (dcs-term)` convention with **English anchors** forced via `attr_l |
| [`DOC-README-CLEANUP`](archive/DOC-README-CLEANUP.md) | ✅ merged (PR #21) | Fix README title, restructure Crate Operations sub-sections, relocate AA System Construction, replace Developer Guide with Documentation links. |
| [`DOC-TECH`](archive/DOC-TECH.md) | delivered | Bilingual `docs/developer/` consolidation (20 EN + 20 FR pages) + workflow page; old sources + `migration/specs/` removed (PR #11). |
| [`DOC-USER-ROLES`](archive/DOC-USER-ROLES.md) | delivered | Bilingual role split of the user guide → `docs/pilot/` + `docs/mission-maker/` (19 EN + 19 FR pages); monolith removed (PR #12). |
| [`FEAT-CONFIG-YAML-COMPLETE`](archive/FEAT-CONFIG-YAML-COMPLETE.md) | merged (PR #65) | Lot 1/3 — runtime: complete `configUser or configDefault` YAML loading (no merge; missing = removed), harden `parseYAML` for the full catalogue + round-trip parity test, bake AA crates into the YAML ( |
| [`FEAT-MOVING-ZONE`](archive/FEAT-MOVING-ZONE.md) | ✅ merged (PR #49) | Resolve CTLD zone positions lazily via `trigger.misc.getZone()` so zones attached to a DCS Moving Zone follow their anchor unit. |
| [`FEAT-USERCONFIG-API`](archive/FEAT-USERCONFIG-API.md) | ✅ merged (PR #45) | Replace broken Section 2 of `CTLD_userConfig.lua` with a safe MM API (`ctld.userSetup` callbacks + helpers), relocate `injectAACrates` to bootstrap, fix all parity bugs in userConfig template. |
| [`FIX-AI-C2-BUGS`](archive/FIX-AI-C2-BUGS.md) | ✅ merged (PR #30) | Two bugs in the AI transport C2 (virtual stock) path: wrong activation guard when a physical vehicle is weight-rejected (Bug 1), and invalid typeName `M1025 HMMWV Armament` in example config causing s |
| [`FIX-CTLD-TOOLS-REVIEW`](archive/FIX-CTLD-TOOLS-REVIEW.md) | merged (PR #69) | Findings from FullGas's manual review of PR #68 — a data-corrupting editor, double-encoded FR text, a dead catalogue key, and three editor gaps |
| [`FIX-I18N-DICT-SYNC`](archive/FIX-I18N-DICT-SYNC.md) | ✅ merged (PR #57) | Fix `$repoRoot` path bug in `generate_i18n_dicts.ps1` (1 level too shallow → looks in `tools/src/`, finds 0 keys); run `-Apply` to sync missing keys; fill FR translations for 60+ missing menu labels. |
| [`FIX-I18N-HARDCODED`](archive/FIX-I18N-HARDCODED.md) | merged (PR #61) | Wrapper dans `ctld.tr()` les chaînes player-facing hardcodées : noms de couches RECON (7) + messages outText AA system (6). |
| [`FIX-LGZ-POLL-NIL-ISFLYING`](archive/FIX-LGZ-POLL-NIL-ISFLYING.md) | ✅ merged (PR #37) | LGZ ground-position poll skips players with `_isFlying=nil` (never flown) — `== false` guard → `~= true`; regression test. |
| [`FIX-LIVE-DCS-FAILURES`](archive/FIX-LIVE-DCS-FAILURES.md) | delivered | Triaged the 10 (of 18) failures from the first live run (2026-07-10) not already fixed by `POST-FULLGAS-FIXES` — all 10 turned out to be cross-scenario state contamination, cleared by a mission reload |
| [`FIX-PLUGIN-CRATE-INSTANT-REFRESH`](archive/FIX-PLUGIN-CRATE-INSTANT-REFRESH.md) | ✅ merged (PR #38) | Immediate Request Equipment refresh on post-init scene crate injection — eliminates the 10s delay between plugin load and crate appearing in menu. |
| [`INTEGRATION-TEST-RUNNER`](archive/INTEGRATION-TEST-RUNNER.md) | delivered | Dependency-free `tools/integration-runner/run_scenarios.py`: discovers + tier-filters scenarios, drives them over dcs-serve REST, polls async ones, writes a JUnit report; `--no-ai` mode runs every `au |
| [`INTEGRATION-TEST-TAGS`](archive/INTEGRATION-TEST-TAGS.md) | delivered | Tier header (`auto` / `auto-check` / `ia`) on the 79 scenarios + 4 templates, and F-122's unresolved verdict fixed. |
| [`POST-FULLGAS-FIXES`](archive/POST-FULLGAS-FIXES.md) | delivered | Applied FullGas's review answers: Feature Q whole-vehicle spawn regression, AI-zone stock validation rewrite, U-108 heliport probe fixes, F-117/F-118 recon fixes, `coord_farp-1` mission static, README |
| [`REINTEGRATE-ORPHAN-TESTS`](archive/REINTEGRATE-ORPHAN-TESTS.md) | delivered | Rebuilt coverage for the 46 features tested only by dead FullGas relics — 6 busted tickets (158 tests, green in CI) + 1 live-DCS ticket (3 scenarios: BCN 30/30, RCN 10/10, VEH 22/22). |
| [`RELEASE`](archive/RELEASE.md) | delivered | `release` skill + `release.yml` (tag `published-v*`); release job moved out of ci.yml (PR #7). |
| [`RELEASE-RC-CHANNEL`](archive/RELEASE-RC-CHANNEL.md) | ✅ merged (PR #41) | Add a pre-release (rc) channel + `published-latest` floating tag to the tag-driven CD (porting VMCT's two mechanics): a `-rc`-suffixed version publishes a GitHub pre-release and leaves `published-late |
| [`SCENE-PLUGINS`](archive/SCENE-PLUGINS.md) | delivered | Pluggable scenes + extracted the mod-dependent Metal FARP into the new [`VEAF/CTLD_plugins`](https://github.com/VEAF/CTLD_plugins) repo (killing its every-mission-start WARN). |
| [`STARTUP-REPORT-INFO-LEVEL`](archive/STARTUP-REPORT-INFO-LEVEL.md) | ✅ merged (PR #58) | Ajouter le niveau `INFO` au `ctld.startupReport` (log-only, sans écho écran) ; migrer INIT-E de `NOTICE` vers `INFO` pour supprimer le bruit des 25 placeholders `extractableGroups`. |
| [`STARTUP-REPORT-UNIFIED`](archive/STARTUP-REPORT-UNIFIED.md) | ✅ merged (PR #56) | Unified startup report: `ctld.startupReport` collector in `CTLD_utils`, `flush()` at end of `ctld.initialize()`, two severity levels (ERROR/NOTICE), always writes `CTLD_STARTUP_REPORT` to `DCS.log`, s |
| [`TEST-PLUGIN-POSTINIT`](archive/TEST-PLUGIN-POSTINIT.md) | ✅ merged (PR #31) | L3 `noPlayer` `auto` scenario (F-124) verifying the post-init plugin contract: `registerSceneModel` after init, `deferMenuSection` direct routing (not queued), `requiresCtld` soft-fail. |
| [`TEST-TYPENAME-VALIDATION`](archive/TEST-TYPENAME-VALIDATION.md) | ✅ merged (PR #36) | Extend `CTLDTypeCollector.collect()` to cover `aiZones[*].vehicleStock`, `capabilitiesByType[*].loadableVehiclesRED/BLUE`, and `aiZones[*].vehicleTypes` — closing the CI type-linter gap exposed by the |
| [`TOOLING-TEST-TAXONOMY`](archive/TOOLING-TEST-TAXONOMY.md) | ✅ merged (PR #29) | Formalise the test taxonomy post CATCH-UP-PILOT-SCENARIOS: update `CONTEXT.md` (tiers `human`/`auto-slow`/`disabled`, L1–L6 levels, headless sweep), create ADR 0006 (`disabled` pattern), fix mt08/mt14 |
| [`TUI-EDIT-MODE-UX`](archive/TUI-EDIT-MODE-UX.md) | merged (PR #63) | Lever l'ambiguïté du modèle d'édition du TUI `ctld-tools` (retours FullGas 2026-07-23) : le TUI édite un **diff** (add/remove/patch contre le catalogue CTLD) mais l'UI le présente comme la liste effec |
| [`USERCONFIG-LOADING`](archive/USERCONFIG-LOADING.md) | delivered | `CTLD_userConfig.lua` removed from build merge; new `CTLD_bootstrap.lua` keeps auto-start in deliverable; userConfig delivered in `dist/` as standalone MM template. |

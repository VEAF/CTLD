# 08 — L6 manual sequences (checklist, no script)

Status: 📋 todo
Type: ia (live DCS, pure observation — no `exec_lua` verdict, follow the checklist)

## Sequences (`tests/manual_test_sequences.md`)

- MT-01 — Multi-group troop transport + disembark menu
- MT-02 — Véhicule entier transport (load / unload / parachute)
- MT-03 — Multi-véhicule entier : load / unload / parachute
- MT-06 — RECON FARP/FOB layer — détection ennemie persistante

## Acceptance criteria

- [ ] All 4 sequences run, each step checked against its pass criteria.
- [ ] Any failure root-caused and fixed; `tests/manual_test_sequences.md` updated if a step is
      stale vs current UI/behavior.
